먼저 색상과 뎁스를 불러오고 카메라 캘리브레이션 기능을 불러오는거 같아 하지만 현재 비슷한 구절의 코드가 반복되어있어서 둘중 하나는 삭제해야할거 같아
색상이나 뎁스값이 없다면 로드 실패라는 출력을 나타내고
self.fx,fy,cx,cy는 카메라 매트릭스에서 추출한 값들이고
이 값들은 3D 좌표를 2D 이미지 평면에 투영하는데 사용돼
마지막으로 왜곡 계수를 저장하는 부분도 있어
이 값들은 렌즈 왜곡을 보정하는데 사용돼
 self.roi는 이미지에서 관심 영역(Region of Interest)을 정의하는데 사용돼
이 영역은 후속 처리나 분석을 위해 집중할 부분을 지정하는데 사용돼

def in roi(self,u,v)는 주어진 2D 좌표(u,v)가 관심 영역 내에 있는지 확인하는 함수야
이 함수는 좌표가 roi의 경계 내에 있는지 여부를 반환해
이 함수는 후속 처리에서 관심 영역 내의 픽셀만 고려하도록 하는데 사용돼

def pixel_to_world(self,u,v,depth_cm)는 2D 이미지 좌표(u,v)와 해당 픽셀의 깊이(depth_cm)를 사용하여 3D 월드 좌표를 계산하는 함수야
이 함수는 카메라 매트릭스와 변환 행렬을 사용하여 2D 좌표를 3D 좌표로 변환해
먼저 깊이를 미터 단위로 변환한 다음, 카메라 좌표계에서의 3D 좌표를 계산해
그런 다음, 변환 행렬을 사용하여 카메라 좌표계에서 월드 좌표계로 변환해
마지막으로 계산된 3D 월드 좌표를 반환해

def extract_objects_dbscan_rotated(self, eps=2.0, min_samples=50)는 DBSCAN 클러스터링 알고리즘을 사용하여 3D 포인트 클라우드에서 객체를 추출하는 함수야
이 함수는 깊이 이미지를 사용하여 3D 포인트 클라우드를 생성한 다음, DBSCAN 알고리즘을 적용하여 클러스터를 식별해
각 클러스터에 대해 최소한의 회전된 경계 상자를 계산하고, 각 객체의 중심 좌표와 크기를 반환해
이 함수는 객체 인식 및 추적에 유용해

points_2d = []는 2D 포인트 리스트를 초기화하는 부분이야
이 리스트는 나중에 3D 포인트 클라우드에서 추출된 객체의 2D 좌표를 저장하는데 사용돼
points_world = []는 3D 월드 포인트 리스트를 초기화하는 부분이야
이 리스트는 나중에 3D 포인트 클라우드에서 추출된 객체의 3D 좌표를 저장하는데 사용돼

for v in range(0,h,2): for u in range(0,w,2):는 깊이 이미지의 각 픽셀을 반복하는 이중 루프야
이 루프는 2픽셀 간격으로 이미지를 샘플링하여 계산량을 줄여
각 픽셀에 대해 깊이 값을 가져오고, 유효한 깊이 값이 있는 경우 2D 좌표와 해당 깊이 값을 사용하여 3D 월드 좌표를 계산해
계산된 2D 좌표와 3D 월드 좌표는 각각 points_2d와 points_world 리스트에 추가돼

clustering = DBSCAN(eps=eps, min_samples=min_samples).fit(points_world)는 DBSCAN 클러스터링 알고리즘을 사용하여 3D 월드 포인트에서 클러스터를 식별하는 부분이야
이 알고리즘은 밀도 기반 클러스터링 방법으로, eps와 min_samples 매개변수를 사용하여 클러스터의 밀도 기준을 설정해
식별된 클러스터 레이블은 clustering.labels_에 저장돼

for label in set(labels):는 식별된 각 클러스터 레이블에 대해 반복하는 루프야
이 루프는 각 클러스터에 속하는 포인트를 추출하고, 최소 회전된 경계 상자를 계산해
각 클러스터의 중심 좌표와 크기를 계산한 다음, 객체 정보를 객체 리스트에 추가해
마지막으로, 추출된 객체 리스트를 반환해

def extract_transparent_rgb(self):는 투명한 물체를 RGB 이미지에서 추출하는 함수야
이 함수는 색상 이미지를 HSV 색상 공간으로 변환한 다음, 특정 HSV 범위를 사용하여 투명한 물체를 마스크로 추출해
마스크를 사용하여 원본 색상 이미지에서 투명한 물체를 분리해내고, 빛과 반사로 인한 잡음을 줄이기 위해 모폴로지 연산을 적용해
최종적으로 투명한 물체가 추출된 이미지를 반환해

def suppress_blue_boxes(blue_boxes, green_boxes):는 파란색 상자 목록에서 녹색 상자와 겹치는 상자를 제거하는 함수야
이 함수는 각 파란색 상자에 대해 모든 녹색 상자와의 겹침 여부를 확인해
겹치는 상자가 발견되면 해당 파란색 상자는 결과 목록에서 제외돼
최종적으로 겹치지 않는 파란색 상자 목록을 반환해